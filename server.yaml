server:
    port: 8080
    logging:
        jsonLogging: true
        logRequests: true

verifier: 
    tirAddress: https://tir.de
    did: did:key:myverifier
logging:
    level: DEBUG
    jsonLogging: true
    logRequests: true

ssiKit:
    auditorURL: http://my-auditor